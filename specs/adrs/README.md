# Architecture decisions

An ADR (Architecture Decision Record) captures a decision that shaped the project: the context, the options considered, and the consequences of picking one. They are written in past tense and treated as historical — new decisions get new ADRs; existing ones get annotated or superseded, not rewritten.

For the "what" and "how" of the architecture, see the architecture overview and how pages under `docs/`. This page is the index of **why** decisions were made.

## Framework ADRs

Decisions that define the `lazy-harness` project itself.

| # | Status | Title | Summary |
|---|---|---|---|
| [001](./001-hybrid-architecture.md) | accepted | Hybrid architecture — framework + dotfile config | Split framework code (package) from personal harness content (user dotfiles). |
| [002](./002-python-uv-distribution.md) | accepted | Python 3.11+ with `uv tool install` | One language, one install command, no compilation step, Windows-feasible. |
| [003](./003-toml-config-format.md) | accepted | TOML config format | Single TOML file parsed by stdlib `tomllib`. No schema file, no DSL. |
| [004](./004-agent-adapter-pattern.md) | accepted | Agent adapter pattern | Thin protocol abstracts the ~6 things the framework actually needs from an agent. |
| [005](./005-xdg-first-paths.md) | accepted | XDG-first path resolution | Single path module with env > XDG > platform-default order. |
| [006](./006-hooks-subprocess-json.md) | accepted | Hooks as subprocess + JSON stdin/stdout | Built-in and user hooks are indistinguishable; agent spawns them, not `lh`. |
| [007](./007-parallel-bootstrap-migration.md) | accepted | Parallel-bootstrap migration | Build the replacement alongside the old system; cut over when proven. |
| [008](./008-compound-loop-async-worker.md) | accepted | Compound loop as async file-queue worker | Stop hook enqueues, detached worker processes. Session close stays instant. |
| [009](./009-profile-symlink-deploy.md) | accepted | Profile symlink deploy | Per-file symlinks from source dotfiles into the agent target dir. |
| [010](./010-pre-compact-preservation.md) | accepted | Pre-compact context preservation | Back up the transcript and distill a working-state summary before compaction. |
| [011](./011-session-export-and-classification.md) | accepted | Session export with classification | JSONL → dated markdown with project/profile frontmatter, atomic writes. |
| [012](./012-sqlite-monitoring.md) | accepted | SQLite monitoring | Idempotent SQLite store, one view module per angle. Six tables today — `session_stats`, `session_attribution`, `ingest_meta`, `sink_outbox`, `loop_events`, `launches`; see the ADR's Evolution section. |
| [013](./013-scheduler-unified-backends.md) | accepted | Unified scheduler backends | launchd / systemd / cron behind one protocol, declared once in config. Complete as of 2026-08-17: the three backends install, uninstall and report state. |
| [014](./014-migration-engine-rollback.md) | accepted | Migration engine with automatic rollback | Detect → plan → execute → auto-rollback on failure. Dry-run is a first-class mode. |
| [015](./015-strict-tdd-workflow.md) | accepted | Strict TDD as a workflow rule | No production code without a failing test first. Non-negotiable. |
| [016](./016-knowledge-dir-qmd-optional.md) | accepted | Knowledge directory + optional QMD | Plain markdown tree; QMD is semantic search opt-in via `shutil.which`. |
| [017](./017-selftest-as-health-check.md) | accepted | Selftest as user-facing health check | `lh selftest` is not pytest — it is the on-machine configuration check. |
| [018](./018-config-discoverability.md) | accepted | Feature discoverability via `lh doctor` + `lh config <feature>` | No wizards on upgrade. `lh doctor` lists features; `lh config <feature> --init` is the opt-in wizard. |
| [019](./019-handoff-session-end-freshness.md) | accepted | Force a final compound-loop evaluation at session end | `SessionEnd` hook + `lh knowledge handoff-now` bypass the Stop-hook gates so the handoff reflects the session's final state. |
| [020](./020-post-compact-context-reinjection.md) | superseded-by: 036 | Post-compact hook re-injects the pre-compact summary | Mirror the PreCompact summary into the live post-compaction context with a 5-minute freshness check. |
| [021](./021-async-response-grading.md) | accepted | Async response grading via the compound-loop worker | One LLM call returns decisions/failures/learnings/handoff *and* a quality grade. Poor grades escalate to PRJ.md. |
| [022](./022-engram-episodic-memory.md) | accepted | Engram as optional episodic memory backend | New `memory/engram.py` wrapper + `[memory.engram]` config + MCP deploy gating. Mirrors the ADR-016 QMD pattern. |
| [023](./023-graphify-code-structure.md) | accepted | Graphify as optional code-structure index | New `knowledge/graphify.py` wrapper + `[knowledge.structure]` config + MCP deploy gating. Mirrors ADR-016 / ADR-022. |
| [024](./024-mcp-server-orchestration.md) | accepted | MCP server orchestration via `lh deploy` | Single seam in deploy writes `mcpServers` to each profile's agent MCP file — `.claude.json` for Claude Code since ADR-032 L2 — from detected tools (QMD, Engram, Graphify). |
| [025](./025-doctor-features-section.md) | accepted | `lh doctor` Features section for triple stack | New `lazy_harness.features` helper + Features section in `lh doctor` listing qmd/engram/graphify with state, version, and pin. First half of ADR-018 implementation. |
| [026](./026-config-wizards.md) | accepted | `lh config <feature> --init` wizards (Fase 3b) | New `lh config` Click group + `wizards/` package with TOML deep-merge. Closes ADR-018 implementation. |
| [027](./027-memory-stack-overview.md) | accepted | Memory stack overview — five-layer model | Names the user-facing layer model that ADR-016/022/023/024 produced. No new mechanism, only canonical vocabulary. |
| [028](./028-classify-rules-configurable.md) | accepted | Configurable session classification rules | Lift the four hardcoded `_classify` rules into typed `[[knowledge.classify_rules]]` config; defaults reproduce historical behaviour. |
| [029](./029-engram-persist-deterministic-mirror.md) | accepted | Deterministic Engram mirror via Stop hook | Deterministic Engram mirror via Stop hook (engram-persist) |
| [030](./030-memory-stack-glue-layer.md) | accepted | Memory stack glue layer — discoverability, freshness, consistency | Seven deterministic glue components closing prompted gaps in the ADR-027 model. No new layer; better integration. |
| [031](./031-default-hooks-merge.md) | accepted | Default hooks merge layer in `lh deploy` | Implicit default hook set merged per-event with user overrides; framework hooks deploy automatically on fresh init. |
| [032](./032-agent-adapter-completeness.md) | accepted | Agent adapter completeness — closing Claude-specific leaks | Four new Protocol methods close seven locations where Claude-specific assumptions bypass the adapter seam. |
| [033](./033-llm-backend-abstraction.md) | accepted | LLM backend abstraction — provider-agnostic inference | New `LLMBackend` Protocol decouples framework-internal inference from the agent CLI choice. Covers Ollama, MLX, and any OpenAI-compatible endpoint. Implemented 2026-06-11. Its config surface and resolution entry point are superseded by ADR-039; the Protocol and backends remain in force. |
| [034](./034-okf-knowledge-producer.md) | proposed | OKF producer — export curated knowledge as an OKF bundle | New `knowledge/okf.py` + `lh knowledge export-okf` transform the `learnings/` layer into a conformant Open Knowledge Format v0.1 bundle. Export-only, non-invasive; episodic logs excluded. |
| [035](./035-capability-registry.md) | accepted | Capability registry — cardinality and external dependency as the two axes | One enumerable registry over the six activation surfaces, classified by cardinality (`one`/`many`) and by whether activation needs an external binary. Explicitly not a public plugin API. |
| [036](./036-compact-hooks-use-real-channels.md) | accepted | Compact-event hooks use the channels the agent actually provides | `PostCompact` has no output channel to the model, so the built-in is removed; `pre-compact` prints plain text into `newCustomInstructions`. |
| [037](./037-metric-event-v2-host-and-workload.md) | accepted | Metric event schema v2 — `host` and `workload` as first-class dimensions | `MetricEvent` gains `host` (resolved at ingest) and a caller-supplied `workload`, carried by a session id `lh exec` pins before spawning. |
| [038](./038-exec-envelope-cost-provenance.md) | accepted | The `lh exec` envelope — cost provenance and the mute failure | A timed-out run is priced from its own transcript, `cost_source` names which door the figure came through, and a failure that says nothing gets a typed `error.kind`. |
| [039](./039-role-routed-inference.md) | accepted | Role-routed inference — one resolution seam, two front ends | `lh exec` and `llm/registry.py` converge on `run_inference`; a named role picks the backend, so local models serve cheap work while Claude keeps the work that needs it. |
| [040](./040-memory-reconcile-and-decay.md) | accepted | Reconcile and Decay — the two missing memory-pipeline stages | `lh memory decay` marks unreferenced learnings `status: superseded` by age horizon, never deletes. `lh memory reconcile` reports `decisions.jsonl` schema drift (deterministic) and, opt-in, contradicting decisions (LLM). Both propose-only; reconcile has no `--apply` at all. |
| [041](./041-multi-agent-hook-contract.md) | accepted | The multi-agent hook contract — a runner that knows its profile | `lh hook <name> --profile <p>` becomes the runner, putting the adapter on the path a hook *takes* and not only the path it deploys through. Support and verdicts are declared per event, hooks declare the signals they need, and `[profiles.<name>].agent` makes a profile an (agent, identity) pair. The freezing gate closed against a throwaway `CodexAdapter` on 2026-09-15, across three runs rather than one: run 2 settled fires/refuses/omitted on 0.67.0, run 3 settled log isolation on the installed 0.67.1. All eighteen builtins are migrated as of 0.67.2, so there is no unmigrated set left to derive a known gap from; the gate asserts coverage of the whole registry instead. |
| [042](./042-multi-file-config-planning.md) | accepted | Multi-file config planning — several targets, explicit deletes, and a refusal instead of a lock | One `plan_config` call returns every write and delete for a profile, and the engine refuses the whole plan if any target's `(mtime_ns, size)` moved between read and apply — a refusal rather than a lock, because the agents do not know the harness exists. Deletion keys on an ownership stamp, not on the file existing. `CodexAdapter` merges `[mcp_servers.*]` into `config.toml` with tomlkit, carrying `[projects.*]` trust and `[hooks.state]` approvals through untouched; hook declarations stay in `hooks.json`, since Codex's trust key is scoped to the declaring file's path. |
| [043](./043-system-docs-by-role.md) | accepted | System docs by role — a set of destinations, and segments that lose their stem | `AgentAdapter.system_docs() -> list[Path]` replaces `system_doc_name() -> str`: destinations the agent loads, not one recognised filename, so a nested or multi-file target becomes expressible and each is separately version-stamped. Segments lose their stem — `head.md`, `_common/common.md`, `_common/<agent>.md`, `tail.md` — and one rendered document is written byte-identical to every destination. [ADR-055](./055-segment-rename-and-the-agent-segment.md) closed the migration window: the legacy stem-keyed fallback is retired and legacy-only profiles are directed through `lh profile migrate`. The sync hook's trigger set is derived from the current roles instead of listed. Decision 10's asset segments stay out: they are `deploy/engine.py`'s, and the assembler half alone would write the document where the agent never reads it. |
| [044](./044-codex-native-edit-path.md) | accepted | Codex's native edit path — four fixes, and the half that cannot be gated | `apply_patch` fires `PreToolUse` with a raw patch blob under `tool_input.command`, the same key `Bash` uses (probe 4c, 0.154.0). Four gaps stacked and each was necessary: the `_TOOL_OPERATIONS` entry, `_shared.EDIT_TOOLS` as the one importable tool-name set, a parser turning the blob into `FileEdit`s, and `pre_tool_use_memory_size`'s tool-name-keyed projection. `command` stays unset for a patch blob, since two builtins scan it for shell syntax. `lh doctor` reports Codex hook trust as `untrusted` / `unknown` / orphaned and never as `trusted`, because establishing that needs the hash the harness declines to recompute. The measured ceiling: Codex's other edit path is `Bash` running a heredoc, which carries no path to gate on, so half of its edits stay ungateable.  |
| [045](./045-credential-boundary.md) | accepted | The credential boundary — a refusal at launch, and a check that names the agent it cannot speak for | A secrets file inside the secrets directory *is* the declaration that a profile carries its own account: a missing one stays fail-open and is reported by `lh doctor`, an unreadable one refuses the launch with `LaunchError("secrets-unreadable")` instead of inheriting another account's credential. `AgentAdapter.credentials_file() -> str \| None` moves the credentials filename off `session_start_preflight`, and `None` gets its own preflight status, `n/a` — "cannot speak for this agent" is not "could not tell". On macOS the file is a mirror of the keychain, so a file-derived `fail`/`warn` for Claude Code degrades to `unknown` while `pass` survives. Enumerating credential variables to scrub was rejected: an incomplete denylist fails open silently, which is the defect itself. |
| [046](./046-delete-is-not-an-edit.md) | accepted | A delete is not an edit — `ToolCall.deletes`, and the readers that must not see it | Probe 6 measured Codex's `*** Delete File:` section (0.154.0), so the delete `_parse_patch` dropped is now expressible. It becomes `ToolCall.deletes: tuple[Path, ...]` rather than an `is_delete` flag on `FileEdit` or an `EditKind` enum: `edits` keeps meaning "the file is still there", so `post-tool-use-format`, `post-tool-use-ansible-lint` and `pre-tool-use-memory-size` cannot act on a ghost by omission, and only `post-tool-use-sync-claude` opts in — a deleted system-doc segment regenerates its tree. `ToolCall.paths` carries the deletes, which is what keeps the path guard gating them; that guard now judges every path instead of `paths[0]`, a gap open since multi-section blobs landed in #348. |
| [047](./047-copilot-adapter.md) | accepted | `CopilotAdapter` — the run-backed subset, and the probe that owns the rest | The third adapter, and the first that ships before its probes. Only rows the design records as `run` or `log` against `copilot 1.0.83` become behaviour: eleven camelCase events (three canonical ones have no Copilot name at all, so they are absent keys), `deny` emitted **top-level and unwrapped** because the `hookSpecificOutput` wrapper is read and ignored, `bash -> RUN_COMMAND` with real shell text, `credentials_file()` `None` on a measured negative. Every `src` row — the edit tool's name, `additionalContext`, the hook document's schema, `copilot-instructions.md` — stays a numbered probe in `specs/designs/copilot-evidence.md` and the tests assert the adapter's silence on it. Ownership is the filename `hooks/lazy-harness.json` under Copilot's `hooks/*.json` glob, not a stamp inside a document whose strictness is unmeasured. |
| [048](./048-codex-rollout-streams.md) | accepted | Codex's rollout is two streams — which one each signal is read from | Step 12's Codex `TranscriptReader`: the rollout is two streams, and each signal declares which of them it is read from. |
| [049](./049-permission-bypass-intent.md) | accepted | Permission bypass is a declared intent, not a forwarded flag | `lh run` was an argv passthrough, so the `lcca` alias shipped a Claude Code flag to whatever binary the profile resolved. The axis becomes three declared positions — `ENABLE` (available, not on), `ACTIVATE` (on, sandbox stays), `NO_SANDBOX` (sandbox too) — and `bypass_argv` answers each per agent. `None` means the agent has no such position and `lh run` exits non-zero naming agent and level; nothing is forwarded, because the levels are ordered by how much they give away and every available substitution is a larger grant than the one requested. Claude Code fills two (`[help]`); Codex fills two, measured against 0.154.0 with a two-target fixture that separates "ran unattended" from "escaped the sandbox" (`[run]`, evidence §7); Copilot fills none. `--dangerously-bypass-hook-trust` is a different axis and is never emitted. |
| [050](./050-metric-event-v3-agent-and-billing-model.md) | accepted | `MetricEvent` v3 — `agent`, and a billing model that admits flat rates | Blast-radius decision 3: `agent` joins `host`/`workload` as a stamped, permanent dimension; billing model is a validated per-profile config field, not read off a credential file or stamped per agent (one agent bills three ways depending on auth mode) — `flat_rate` short-circuits pricing to `cost=0.0, cost_source="subscription"` and never fires `unknown_models`. Every view renders a flat-rate bucket's cost as `—`, never `$0.00`, and relabels a mixed total *priced only*. |
| [051](./051-transcript-reader-does-not-meter.md) | superseded-by: 053 | The `TranscriptReader` Protocol does not carry what metering needs | Measured over 2,619 real transcripts and 102,464 usage records: `ClaudeCodeAdapter.read()` and `collector.iter_assistant_messages` agree on every token bucket to the token, and the reader answers one of the five questions ingest asks a transcript. It has no model (6 distinct vs 0 — `session_stats` is `UNIQUE(session, model)`), no message id (dedup across a `/resume` rewrite needs one), one cache-creation field where pricing needs the 5m/1h split, and `locate_sessions` yields the `memory/` logs ingest excludes. So the hand parser stays and Codex is not metered: `ingest` skips a dialect it cannot parse and `lh doctor` names it, rather than reporting the profile empty. A pseudo-model was rejected — $0 for real spend merges "free" with "unknown". The Protocol change that would close it is written out in the ADR; `tests/unit/test_transcript_reader_parity.py` asserts the equality and all four differences so the decision cannot go stale quietly. |
| [052](./052-profile-assets-per-agent.md) | accepted | Profile assets are deployed per agent — segments, file-level links, an ownership ledger | Multi-agent design decision 10: `deploy_profiles` symlinked every entry of `profiles/<p>/` unfiltered, so a Codex profile received Claude Code's `settings.json`. The source becomes three ordered layers — root, `shared/`, `<agent>/` (named by `list_agents()`) — and the root staying a *layer* is what keeps an unmigrated profile deploying exactly as before. A name carried by two layers is walked and linked per file so the segments merge instead of the second link replacing the first; a name carried by one is still linked whole. Collisions are returned as data and printed by the deployer, never resolved in silence. Deploy records its links in `<config_dir>/.lazy-harness/links.json` and retracts the ones it no longer generates (ADR-046's delete case, applied to symlinks), adopting on a first run with no ledger and treating an unparseable one as absent because re-adopting is the recoverable direction. `lh profile migrate` routes by `config_targets()` and reads `system_docs()` as a *root* marker, since `sync_agent_md` writes the assembled doc at the root and moving it would leave the assembler writing where the deploy no longer links. |
| [053](./053-transcript-reader-carries-metering.md) | accepted | The `TranscriptReader` carries what metering needs, and ingest reads every agent through it | Reverses ADR-051 on the terms ADR-051 set: its `not hasattr` tripwires fired. `TranscriptEvent` gains `model` and `message_id`, `TokenUsage` gains `cache_creation_1h_tokens` — appended with defaults, `None` never `0`, and the two cache fields are siblings priced at different rates with the breakdown winning where it disagrees with the total. Codex's model comes from `turn_context` on an earlier line, carried forward in the read generator and never on the adapter, which `registry.py` shares across profiles; its dedup key is `response_id` (unique across 176 records) and not `turn_id` (21 distinct for those 176). Ingest drops `_PARSED_DIALECT` for `isinstance(agent, TranscriptReader)`, so an agent that can be read is metered whatever it is called, and `lh doctor` says `reads` again. Session and project come from a second, optional `TranscriptIdentity` Protocol — split from `TranscriptReader` because `isinstance` against it gates `transcript_health` and `stop_verify_guard`. The hand parser stays: `lh exec` prices through it, so an integration test runs both paths and compares the stored row. The `memory/` exclusion stays with the consumer, asserted from both ends. |
| [054](./054-external-hook-placeholders.md) | accepted | `[hooks.*].external` gains `{profile}` / `{config_dir}` placeholders | `_hook_entries_for` copied an `external` hook's command verbatim to every profile, so a command needing its own profile's directory — the herdr `SessionStart` hook that fell out of version control at ADR-041 — had no way to say so. `ExternalHookConfig.command` now expands `{profile}` / `{config_dir}` per profile, in the same loop that already resolves the harness's own `hook_command`; `{config_dir}` is the profile's raw, unexpanded field (`~/.claude-lazy`, not the resolved absolute path) for the same chezmoi-portability reason `hook_command` itself never embeds a home directory. A placeholder outside that pair is refused with a diagnostic naming it. A per-profile `[profiles.<name>.hooks.*]` table was rejected as speculative generality nothing measured needs. |
| [055](./055-segment-rename-and-the-agent-segment.md) | accepted | Closing ADR-043's migration window — who renames the segments, and who owns the assembled doc | ADR-043 shipped the role-named segments with a read fallback to the stem-keyed spelling and called it a migration window; nothing closed it, because `lh profile migrate` moved a profile's *assets* and left the doc segments at the root without renaming them. `migrate` now renames them too — `CLAUDE.head.md` → `head.md`, `CLAUDE.tail.md` → `tail.md`, `_common/CLAUDE.common.md` → `_common/common.md` — as a separate list on the plan with its own verb and its own count, under the same all-or-nothing guard, stems from the registry rather than a typed list. The shared segment waits until no sibling directory still carries `<stem>.head.md`, read from disk not `config.toml`, because `sync_profiles` loads every shared segment before its first write and a premature rename takes the whole tree down; that is also why a new role-named profile is added *after* the shared rename, not before. A legacy file beside its role-named replacement is reported as a leftover rather than renamed over, and two spellings claiming one role are refused by name before anything moves. `_common/<agent>.md` is unchanged from ADR-043 — shared, keyed by `agent_for_profile` — with a test that the key is load-bearing across agents, so a line living only in `claude-code.md` never reaches a Codex document. The assembled `CLAUDE.md`/`AGENTS.md` leaves the chezmoi source: it is generated, version-stamped and regenerated by the sync hook, and two writers on one file lose silently. |
| [056](./056-codex-honours-claude-shaped-matchers.md) | accepted | Codex honours Claude-shaped `PreToolUse` matchers, and the deployed groups stay agent-agnostic | The F9 run of 2026-09-17 12:32 approved 31 hooks and still let a recursive delete through, and a matcher literal naming five Claude Code tools was the leading suspect on a Codex profile whose native edit tool is `apply_patch`. Two probes closed it. Codex evaluates a `PreToolUse` matcher as a **regex with working anchors** (`^Bash$` fired on `Bash`, not on `apply_patch`), against the native name **and the Claude-compatible alias** (`Edit|Write` fired on `apply_patch`), and it evaluates **every** group rather than stopping at the first match — so the eight groups the harness deploys are not suppressed. The hook-exec probe then falsified the four remaining candidates: the deployed bare `lh` resolves in the hook process, exits 0 with a valid deny envelope, receives the payload shape `codex-evidence.md` §1 records, and Codex honours the verdict — surfacing it in its **approval-review stage**, on stderr, while the `--json` stream carries only the model's prose about it. Matchers therefore stay declared once per builtin in `hooks/loader.py` and shared across agents; per-agent literals would fork that registry to express a scoping decision that does not vary by agent. `#382` changed no deployed group despite its subject — its diff is probes, the gate, the trace and tests — and `LH_HOOK_TRACE` is what it did ship for the deployed path, landing under the agent's runtime dir (`CODEX_HOME` before the profile's `config_dir`, ADR-032 L3), not under the profile's log as its docstring claimed. Probe 6 then ran (2026-09-17 16:41) and closed the question the ADR left open: the guard was **invoked** under the real trust store and answered allow, and the deployed `hooks.json` was byte-identical across the run, so neither trust nor the deployment was the delta — the 12:32 FAIL was a fired-but-allowed on a spelling the rule permitted by design. The ADR's **Evolution** section records it, with the two decisions that followed: the recursive-delete rule denies on recursion alone (#385), and F9 phase B replays the issued command through the shipped guard and judges the agent against that verdict (#387). The third delta, the driver, closed on **run 4** of the acceptance gate (2026-09-18 08:27, `lh` 0.71.1): 17 assertions, 0 failed, 0 blocked, and the two not-observed closed on the run's own recorded artifacts rather than on a fifth run. |
| [057](./057-codex-last-refresh-is-not-liveness.md) | accepted | Codex `last_refresh` is freshness evidence, not a credential verdict | Codex's measured auth file has a refresh timestamp but no expiry or rejection state. No age can therefore prove a login live or dead. The preflight keeps the adapter's `None` / `n/a` answer; a future read-only status contract may reopen the decision, while raw age may only be labelled telemetry. |
| [058](./058-keychain-mdat-is-operator-only.md) | accepted | Keychain `mdat` remains operator-only evidence | An Aqua probe proved the non-secret modification date distinguishes a fresh Claude Code keychain item from its stale file mirror. It did not make Keychain access safe in an agent hook: agent panes also descend from Aqua, so the required execution guard is unenforceable. Lazy-harness never invokes `security`; macOS mirror failures remain `unknown`. |
| [059](./059-portable-skills-native-commands-agents.md) | accepted-deferred | Portable skills are projected; commands and agents remain native | Skills are portable after an adapter places them in a measured discovery root; commands and subagent definitions retain runtime-specific semantics and stay in agent segments. The trigger was met on 2026-09-19; Wave 1 will project resolved profile skills with ledger ownership and whole-plan collision refusal for Codex's global `~/.agents/skills`. |
| [060](./060-agents-md-is-the-portable-repository-contract.md) | proposed | `AGENTS.md` is the portable repository contract | Shared repository rules move to `AGENTS.md`; `CLAUDE.md` imports them and keeps only its native appendix. Three pilot repositories and live root/nested probes gate the fleet migration. |
| [061](./061-billed-cost-and-api-equivalent-cost-are-separate.md) | proposed | Billed cost and API-equivalent cost are separate | Subscription usage keeps billed cost absent while a separately labelled, versioned API-equivalent price may be computed per response. Unknown models and context tiers remain explicit nulls. |
| [062](./062-session-end-publishes-bounded-project-state.md) | proposed | SessionEnd publishes bounded project state | Successful sessions replace one opt-in generated PRJ section; they never rewrite human-curated state. Real writes bump `updated`, while absent or ambiguous projects remain logged no-ops. |

### Status values

Each active ADR carries one of the following statuses in its header. The column above is the index view; the ADR file is the source of truth.

| Status | Meaning |
|---|---|
| `accepted` | Decision taken **and** embodied in code, config, or tests. Default state for a shipping decision. |
| `accepted-deferred` | Decision taken and locked, but not yet embodied. Before its trigger it is intentionally unscheduled; once met, the ADR records the scheduled lane and stays deferred until it ships. |
| `proposed` | Written and reasoned, but not yet committed to. Open for revision. ADR-034 and ADR-060 through ADR-062 currently hold this status. |
| `superseded-by: NNN` | Replaced by a later ADR. The record is kept for history; the pointer names its replacement. ADR-020 currently holds this status. |

New decisions default to `accepted` once they ship. A decision that turns out wrong is **superseded** by a new ADR rather than edited in place.

## Legacy ADRs

The ADRs numbered 001–013 under `../archive/adrs-legacy/` preserve the predecessor project's decision history verbatim. They are kept as archival material for provenance; they are intentionally not edited to match the current nomenclature or architecture.

They live outside `docs/` entirely, so they never surface in the rendered public site. Browse them on the repo if you want the history of how the framework got to its current shape.
